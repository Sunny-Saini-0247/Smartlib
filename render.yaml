services:
  - type: web
    name: smartlib-library
    runtime: python
    buildCommand: pip install -r requirements.txt && python download_models.py
    startCommand: gunicorn --workers 1 --threads 4 --timeout 120 app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.13.5
